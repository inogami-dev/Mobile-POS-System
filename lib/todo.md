# TODOs

## Planned Features

- Registering a product
[] Add a feature where if the user scan the newly bought products and it appears to have an identical barcode/qrcode the already registered information of the similar product will appear except for the quantity and cost, so that the user can register (append) new batch of the same product without affecting the already in stock ones.
[] There should be a separate collection of the stored product just in case there is a change in cost when acquiring it. So later on we can compare the cost between aquisition. [id, productName, barcode, cost, price, dateTime, registeredBy]
